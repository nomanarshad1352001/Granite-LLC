// This presentation intentionally runs without a database.
// Replace the typed mock repositories in src/lib with a real data adapter
// when persistent production storage is required.
export const demoDataMode = true;
